name: Code coverage

on: [push]
jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install pipenv
        run: pipx install pipenv

      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pipenv"
          architecture: "x64"

      - name: Install dependencies
        run: pipenv sync --dev

      - name: Run Test
        run: |
          pipenv run coverage --cov-report=xml

      - uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: true
          verbose: true
